> fix default height on export
> toggle color/no color on export
> add react component export option
> add export collection as folder (icons/[...icons.jsx][index.js]) for use with @icons or import 'icon' from 'icons' (vite)
